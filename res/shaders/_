#extension GL_EXT_nonuniform_qualifier : require
layout (set = 0, binding = 1) uniform sampler2D textures[];
    vec4 textureColor = texture(sampler2D(texSamplers[nonuniformEXT(outTexIndex)]), fragUV);